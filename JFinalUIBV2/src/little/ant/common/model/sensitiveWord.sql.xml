<?xml version="1.0" encoding="UTF-8"?>

<sql-statement namespace="common.sensitiveWord">
	
	<!-- 分页查询：select -->
	<sql id="splitPageSelect">
		<![CDATA[ 
        	select * 
    	]]>
	</sql>
	
	<!-- 分页查询 ：from -->
	<sql id="splitPageFrom">
		<![CDATA[ 
        	from common_sensitiveword
			where 1=1 
			
			<% if(!isEmpty(sensitiveWord)){ %>
					and sensitiveWord like #'%$sensitiveWord$%'#
			<% } %>
			
			<% if(!isEmpty(replaceWord)){ %>
					and replaceWord like #'%$replaceWord$%'#
			<% } %>
    	]]>
	</sql>
	
</sql-statement>