<?xml version="1.0" encoding="UTF-8"?>

<sql-statement namespace="pingtai.menu">
	
	<!-- 查询根菜单 -->
	<sql id="root">
		<![CDATA[ 
        	select ids, ${names}, isparent, images from pt_menu where parentMenuIds is null and systemsIds = ? order by orderIds asc
    	]]>
	</sql>
	
	<!-- 查询子菜单 -->
	<sql id="child">
		<![CDATA[ 
        	select ids, ${names}, isparent, images from pt_menu where parentMenuIds = ? order by orderIds asc
    	]]>
	</sql>
	
	<!-- 查询子菜单数量 -->
	<sql id="childCount">
		<![CDATA[ 
        	select count(*) as counts from pt_menu where parentmenuids = ?
    	]]>
	</sql>
	
</sql-statement>